export type Stat = {
  value: string
  key: string
}

// TODO(stats): interim true facts — replace with management-approved numbers when received
export const STATS: Stat[] = [
  { value: '+40', key: 'stats.0' },
  { value: '3', key: 'stats.1' },
  { value: '+15', key: 'stats.2' },
]